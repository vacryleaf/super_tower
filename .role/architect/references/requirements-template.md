# 功能需求文档模板

状态：模板。

复制本模板到 `docs/requirements/YYYY-MM-DD-<feature-slug>.md`，删除不适用提示，并把所有未决信息保留在“待确认问题”中。

## 1. 文档信息

- 功能名称：
- 当前状态：待确认
- 提出背景：
- 目标版本或里程碑：
- 关联领域：
- 相关文档：

## 2. 背景与目标

### 背景

说明当前用户痛点、系统限制和触发场景。

### 目标

- [ ] 用可观察行为描述目标。
- [ ] 说明成功后用户能完成什么。
- [ ] 说明不改变哪些既有规则。

### 非目标

明确本次不处理的内容，避免把需求扩展成无边界重构。

## 3. 用户流程与规则

### 用户流程

```text
前置条件 -> 用户意图 -> 系统响应 -> 状态变化 -> 后续入口
```

### 规则定义

| 规则 | 当前值或行为 | 新需求 | 来源或依据 |
| --- | --- | --- | --- |
| 触发时机 |  |  |  |
| 目标选择 |  |  |  |
| 数值和单位 |  |  |  |
| 持续时间 |  |  |  |
| 堆叠语义 |  |  |  |
| 失败处理 |  |  |  |

## 4. 架构归属

### 结论

- 首要架构层：
- 次要架构层：
- 现有可复用入口：
- 不应修改的层：

### 模块影响表

| 层次 | 文件 | 方法、Schema 或数据入口 | 变更类型 | 职责 |
| --- | --- | --- | --- | --- |
| 数据定义 |  |  | 新增/修改/不变 |  |
| 规范化与校验 |  |  | 新增/修改/不变 |  |
| 状态转换 |  |  | 新增/修改/不变 |  |
| 行为决策 |  |  | 新增/修改/不变 |  |
| 战斗流程 |  |  | 新增/修改/不变 |  |
| 状态与触发 |  |  | 新增/修改/不变 |  |
| 成长与持久化 |  |  | 新增/修改/不变 |  |
| UI |  |  | 新增/修改/不变 |  |
| 测试与文档 |  |  | 新增/修改/不变 |  |

## 5. 数据契约

### 新增或修改字段

| 字段 | 类型 | 必填 | 默认值 | 合法范围 | 迁移策略 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 引用关系

- 来源 ID：
- 被引用数据：
- 命名空间要求：
- 缺失引用处理：
- 是否影响图鉴索引：

### 运行时状态

- 写入哪个运行时字典或状态：
- 由哪个服务解释：
- 是否使用 `effects`、`conditional_effects`、`tick_effects` 或 `triggers`：
- 是否需要事件常量或新 action：

## 6. 调用链与状态流

```text
输入或数据 -> 规范化 -> 状态转换 -> 流程编排 -> 效果/触发结算 -> BattleState/存档/UI
```

说明每一步的输入、输出和失败分支。实时战斗功能必须说明 `PlaySession`、`BattleService`、相关解释器和服务如何保持一致。

## 7. UI 影响

- 入口页面：
- 展示状态：
- 用户意图：
- 加载和空状态：
- 错误提示：
- UI 不负责的规则：

## 8. 持久化与兼容性

- 是否写入 Profile：
- 是否写入局内存档：
- 保存稳定 ID 还是规范化快照：
- 旧存档迁移：
- 缺失内容或版本不兼容：
- Windows/macOS 路径和命令影响：

## 9. 边界条件与风险

- [ ] 无目标、目标死亡或目标被移除。
- [ ] 状态重复、叠加、刷新和到期。
- [ ] 同一事件触发多个效果的顺序。
- [ ] 战斗结束、回合结束和存档时机。
- [ ] 数据缺失、未知 action、非法引用和版本不匹配。
- [ ] 教程、正式楼层、精英和 Boss 的差异。
- [ ] UI 重复点击、重新进入页面和加载失败。
- [ ] 与旧字段或历史数据的兼容风险。

## 10. 测试矩阵

| 层次 | 测试场景 | 预期结果 | 测试文件或新增位置 |
| --- | --- | --- | --- |
| 数据校验 |  |  |  |
| 运行时状态 |  |  |  |
| 战斗规则 |  |  |  |
| 行为决策 |  |  |  |
| 成长与存档 |  |  |  |
| UI 冒烟 |  |  |  |
| 跨平台入口 |  |  | `run_tests.sh` / `run_tests.bat` |

## 11. 验收标准

- [ ] Given ... When ... Then ...
- [ ] Given ... When ... Then ...
- [ ] 现有功能回归通过。
- [ ] 相关文档和图鉴字段已同步。
- [ ] 未引入模拟战斗路径或 UI 规则分支。

## 12. 实现顺序

1. 确认规则、Schema 和待确认问题。
2. 添加或修改数据定义及校验。
3. 接入状态转换、解释器或行为决策。
4. 接入流程、持久化和 UI。
5. 添加测试并运行跨平台入口。
6. 回写文档状态并提炼稳定知识。

## 13. 待确认问题

| 问题 | 影响 | 默认假设 | 决策人 | 状态 |
| --- | --- | --- | --- | --- |
|  |  |  |  | 待确认 |
