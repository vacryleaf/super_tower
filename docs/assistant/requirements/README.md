# 需求文档

状态：持续维护。

本目录用于保存经过架构分析的功能需求文档和开发任务单。文档以 Markdown 为准，文件名建议使用：

`YYYY-MM-DD-<feature-slug>.md`

每份文档必须标明：

- 当前状态：`待确认`、`已确认待实现`、`实现中` 或 `已实现`
- 目标和非目标
- 受影响的架构层、文件和数据 Schema
- 验收标准与测试矩阵
- 待确认问题、假设和兼容性风险

需求文档不是代码实现记录。实现完成后，应根据代码和测试结果回写状态，并把稳定的架构经验提炼到顾问 Skill 的知识参考中。
