# 回合顺序设计

状态：已实现。

- 战斗开始：建立 Combatant、应用战前状态/特性、处理先手和首击。
- 行动顺序：回合开始事件 → 资源/冷却更新 → 按敏捷选择当前行动者 → 执行动作 → 处理伤害与触发。
- 行动者不再区分固定的玩家阶段和敌方阶段；敌方选择仍由行为规则负责，玩家行动仍由 UI 输入负责。
- 回合结束：持续伤害/恢复、状态持续时间、触发器、日志和胜负检查。

敏捷决定先后，不参与敌人属性倍率；额外攻击段数必须作为独立命中事件处理。
